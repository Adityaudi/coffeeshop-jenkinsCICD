
pipeline{
    agent any
    
    parameters { 
        text(name: 'BUILD Coffeeshop Project', defaultValue: '')
        choice(name: 'BUILD PROJECT', choices: ['NOT OKE!', 'OKE!'])
        choice(name: 'DEVLOPMENT PROJECT', choices: ['NOT OKE!', 'OKE'])
        booleanParam(name: 'YES, IM AGREE!', defaultValue: false,)
    }

    stages{
        stage("Build project"){
            steps {
                echo "====++++something++++===="
            }
        }
    }
}